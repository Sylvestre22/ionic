export interface UserRegister {
    avatar: string;
    username ? : string;
    first_name: string;
    last_name: string;
    phone: string;
    email: string;
    password: string;
    confirm_password: string;
}